apiVersion: v1
kind: Service
metadata:
  name: {{ include "md5generator.fullname" . }}
  labels:
    {{- include "md5generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "md5generator.selectorLabels" . | nindent 4 }}
